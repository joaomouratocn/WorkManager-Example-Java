<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.programadorjm.workmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkManager">
        <activity android:name=".ParallelWork"></activity>
        <activity android:name=".OnTimeRequestWorkWithData" />
        <activity android:name=".ConditionsWorks" />
        <activity android:name=".ChainableWork" />
        <activity android:name=".PeriodicWork" />
        <activity android:name=".OnTimeRequestWork" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>